# MissingData.yml
model: pg
version: '2.1'
name: Missing forms in screening, Baseline and safety
template: site_events_count_data_missing
report:
  events:
    - Screening
    - Baseline
    - Common
